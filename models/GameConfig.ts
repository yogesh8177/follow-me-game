export default class GameConfig {
    clientPort: number;
    serverPort: number;
    serverUrl: string;
    maxScore: number;
    minScore: number;
    timeOutInSeconds: number;
    maxTimeoutMisses: number;
    isServer: boolean;
}